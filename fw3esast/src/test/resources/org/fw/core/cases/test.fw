
(do
 (: a test-mod.test-value)
 (: b (usem test-mod
     test-value
 ))
 (: c (usec (call ModuleCompEnv.constructor test-mod)
     test-value
 ))
 (call eq c b)
)


(invoke! _PrintHelloWorld)
(do
 (: obj (invoke! (call _CreateNewObjectOperation 51)))
 (invoke! (call _WriteOperation obj (call (invoke! (call _ReadOperation obj)) (symbol "+") 10)))
 (invoke! (call _ReadOperation obj))
)

(invoke! _ReadLine)

(do
 (call 5 (symbol "+") 5)
 
 (call Module.constructor [
     (call Declared.builder (symbol "use-comp-env") 0)
     (: zero 0)
 ])
 // (usec <CEnv> <...>) // use a comp env on top of your current cenv stack
 // (usem <Module> <...>) // use a module as comp env (basically import)
 // (telephonize <val>) // erase type information (wrap with telephonist that behaves exactly the same, basically x -> (telephonist arg (call x arg)))

 (call eval-vit VitVar.instance) // this literally creates a new telephonist with given code, why would you need anything else

 (: func (call Function.builder
           (call Constraint.builder
                (compile-vit (call (var) (symbol "size")))
                (compile-vit 2)
           )
           (compile-vit (call (var) (symbol "size")))
           (var)
       ))

 (: a 2)
 (: b 1)
 (call a (symbol "+") b)

 (call func (symbol "fn-call") [0 1])
)
Symbol
