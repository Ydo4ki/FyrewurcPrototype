
(: StaticFnSignature (struct
    (= parameters (Array Constraint))
    (= result Constraint)
))

(: StaticLambdaSignature (struct
    (= parameter Constraint)
    (= result Constraint)
))

(: LiquidFnSignature (struct
    (= first Constraint)
    (= errr ok I don`t know how to describe that as a single struct, that`s gotta be a chain of functions or smth)
))

// paper notes are cool
// Ok
// Actually I think we can add mutable states
// and time
// and
// so many more concepts
// that usually exist by default
// time will be local as well btw
// which is cool if you think about it