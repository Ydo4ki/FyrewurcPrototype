//(== toExpr (call BoxType (Symbol "constructor") (Symbol "to-expr")))
//(== toExpr toExpr)
//(call BoxType (Symbol "constructor") (Symbol "to-expr"))
(Test
    (== unspecified unspecified)
    (== toExpr (call BoxType (Symbol "constructor") (Symbol "to-expr")))

    (== toExpr (get BoxType constructor to-expr)) // oh very nice
    // too nice I would say
    // I'd rather go with something like
    (== toExpr (call (get BoxType constructor) (Symbol "to-expr"))) // lmao
    // there should be a proper compilable constructor tho
    // but whatever
)

// to-do
// let
// maybe merge context with rt env
// add '.'
// allow modules refer to their own elements
// vit and expr functions
